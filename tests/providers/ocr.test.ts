import { describe, it } from "vitest"

describe.skip("OCR enqueue with provider context", () => {
  it("should enqueue job with provider_id", async () => {
    // Requires redis, db, minio; left as e2e smoke in acceptance script.
  })
})
