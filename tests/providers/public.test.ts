import { describe, it } from "vitest"

describe.skip("public provider endpoint", () => {
  it("returns brand info", async () => {
    // Would call /api/providers/angebae and assert brand_settings.
    // Covered by acceptance script instead.
  })
})
